package com.kuba86.letsEntryptScript

import com.kuba86.letsEntryptScript.model.RunOptions
import os.*
import scribe.*

class Run(options: RunOptions, domains: Option[List[String]] = None) {
  info("run command executed")
}
