[project]
name = "ansible-deployment"
version = "0.1.0"
requires-python = ">=3.14"

[dependency-groups]
ansible = [
    "ansible==14.4.0",
    "ansible-navigator==26.9.0",
]

[tool.uv]
package = false
